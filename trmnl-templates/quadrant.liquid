<div class="view view--quadrant">
  <div class="layout">
    <div class="q-content">
      <span class="q-quote">{{ quote }}</span>
      <span class="q-reference">BOOK {{ season }}, CHAPTER {{ episode }}</span>
    </div>
  </div>
</div>

<style>
* { box-sizing: border-box; margin: 0; padding: 0; }
html, body { width: 100%; height: 100%; font-family: sans-serif; }

.view--quadrant {
  display: flex;
  flex-direction: column;
  width: 100%;
  height: 100%;
  overflow: hidden;
}
.layout {
  flex: 1;
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 1rem 1.25rem;
  overflow: hidden;
}
.q-content {
  display: flex;
  flex-direction: column;
  gap: 0.5rem;
  width: 100%;
}
.q-quote {
  font-style: italic;
  font-size: clamp(12px, 2.5vw, 17px);
  line-height: 1.35;
  word-break: break-word;
  display: -webkit-box;
  -webkit-line-clamp: 4;
  -webkit-box-orient: vertical;
  overflow: hidden;
}
.q-reference {
  font-style: italic;
  font-size: 0.65rem;
  font-weight: bold;
  text-transform: uppercase;
  letter-spacing: 0.03em;
}
</style>